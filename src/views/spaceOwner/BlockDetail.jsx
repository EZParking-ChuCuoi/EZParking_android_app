import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const BlockDetail = () => {
  return (
    <View>
      <Text>BlockDetail</Text>
    </View>
  );
};

export default BlockDetail;

const styles = StyleSheet.create({});
